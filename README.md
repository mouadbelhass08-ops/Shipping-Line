An attempt to wrestle with Nodejs's logic by creating a prototype of a web application featuring a shipping line management system.
To run using vs code, just open the terminal and run : node server (all nodejs modules are locally available)
